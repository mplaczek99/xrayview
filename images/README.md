# Sample Assets

This directory contains local image assets used for testing and detector tuning.

- `BMP/`
  - Bitmap bitewing x-ray scans used as source image inputs.
- `PNG/`
  - Preview and reference images used by tests and detector tuning.

Notes:

- The assets in this directory are ordinary image files.
