{
  "name": "xrayview",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm --prefix frontend-app install",
    "dev": "npm --prefix frontend-app run dev --",
    "tauri": "npm --prefix frontend-app run tauri --",
    "tauri:dev": "npm --prefix frontend-app run tauri:dev --",
    "tauri:build": "npm --prefix frontend-app run tauri:build --"
  }
}
