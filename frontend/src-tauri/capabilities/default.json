{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the XRayView frontend shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
