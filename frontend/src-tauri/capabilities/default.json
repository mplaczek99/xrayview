{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop capability for the main XRayView window. The frontend relies on app-local custom commands only.",
  "windows": ["main"],
  "permissions": []
}
